version: '3.8'

services:
  agent-fullstack:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: data-agent-runtime
    ports:
      - "3000:3000"
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./chroma_data:/app/chroma_data
    restart: unless-stopped